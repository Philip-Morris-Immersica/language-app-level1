/**
 * The textbook block on pp. 76–97 has no workbook section, so „Преговор“ stays
 * empty for lessons 11–15 — `test-b1-1` is the review for the whole block.
 */
import type { Exercise } from '@/content/types';

export const workbookExercises: Exercise[] = [];
