/**
 * STUB — being rewritten from the textbook block on pp. 76–80.
 *
 * The previous version covered only a fraction of the textbook page and is
 * intentionally cleared rather than patched. Fill this in from
 * `../_BRIEF-b1-lesson-11.md`; the loader in `src/content/b1/index.ts` stays
 * commented out until the lesson is complete.
 *
 * `id`, `number` and `title` are fixed by the UNHCR syllabus — do not change.
 */
export const metadata = {
  id: 'b1-lesson-11',
  number: 11,
  title: 'Природата и географията на България',
  description: '',
  grammarTopics: [] as string[],
  vocabulary: [] as string[],
};
