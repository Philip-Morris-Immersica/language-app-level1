# HANDOFF — alex (B1) → philip → master

**Режим:** план / изпълнение по фази. Всеки нов чат чете **само своята секция** + „Факти“. Не разглеждай уроци. Не чети `exercises.ts`.

**Клонове (към 2026-08-18):**
- Текущ checkout: `philip` (clean, up to date с `origin/philip`)
- `philip` е **21 комита назад** от `master`, **0 уникални** → `philip` е пълен ancestor на master. Нищо не е изгубено; просто не е ре-синкнат след PR #11. Ре-синк: `git checkout philip; git merge origin/master`.
- `alex` е **40 комита пред** `master`, `master` е **26 пред** `alex`
- Merge-base: `b709f6e`
- Алекс последно е мърджвал master в `6eddcaf` (отдавна)
- Локалният `alex` в тази работна директория е **143 комита назад** от `origin/alex` — игнорирай го, работи от `origin/alex`.

---

## Факти (не преоткривай)

### Какво носи Алекс
- Уроци **01–10 дигитализирани** + TTS/assets. **11 = само `.gitkeep`**, няма loader.
- Loaders: 01–10. Counts: 01=19, 02=14, 03=18, 04=10, 05=21, 06=7, 07=24, 08=12, 09=31, 10=8
- Нови B1 компоненти в `src/content/b1/components/` (домейнът му — OK)
- Нови префиксирани типове: `b1-illustrated-cards-grouped`, `b1-match-pairs-dragdrop`, `b1-grammar-table`, `b1-grammar-examples`, `b1-sort-to-columns`, `b1-info-highlight`
- `src/i18n/b1.ts`, `types.ts`, `exercise-components.ts`, `vocabulary-augment.d.ts`
- ~1300 asset файла (mp3/картинки) — не ги чети
- Няма тестове B1 (`B1_TEST_LOADERS` празен)

### Файлове ИЗВЪН B1 домейна (един)
`scripts/generate-tts.ts` — **+142/−14**. И **master също го е пипал** (3 комита). Реален конфликт.

Какво е пипнал Алекс в скрипта (НЕ мърджвай as-is):
1. Сменя **глобалния** `GEMINI_PROMPT` (би чул A1/A2 TTS при реген)
2. Твърдо кодира B1 `ttsPrompt` override-и в скрипта (трябва да са в content)
3. Импорт `expandVocabAbbreviations` от `@/lib/tts` — **функцията я няма** нито в master, нито в `src/lib` на alex (само локален блок в скрипта)
4. `READING_TEXT_EXCLUDE` за `lessonNum === '03'` / `l03-ex-31` (A1 риск)

**Решение по подразбиране:** при конфликт вземи **master** версията на `generate-tts.ts`. B1 MP3-тата вече са генерирани. Override-ите в content са follow-up, не блокер.

### Дребни конфликти в B1 content
Master е добавил само 3-редов `content-lint-disable no-free-writing` коментар в:
- `src/content/b1/lessons/b1-lesson-01/exercises.ts`
- `src/content/b1/lessons/b1-lesson-02/exercises.ts`

Алекс е преписал файловете **без** този коментар. Резолюция: съдържанието на Алекс + върни коментара ако lint пак пищи.

### Блокер #1 — renderer leak (по-важно от git конфликта)

`ExerciseRenderer.tsx` прави плосък merge:

```ts
const CUSTOM_RENDERERS = { ...A2_CUSTOM_RENDERERS, ...B1_CUSTOM_RENDERERS };
const CustomRenderer = CUSTOM_RENDERERS[exercise.type]; // БЕЗ проверка на нивото
```

Алекс override-ва **непрефиксирани** shared типове:
`table_fill`, `reading_text`, `dialogues`, `workbook_fill_blank`, `grammar_examples`, `grammar_table`

Защото B1 се spread-ва **след** A2, при мърдж B1 компонентите ще заменят:
- A1 `reading_text` / `dialogues` / `grammar_table` / …
- и вече съществуващите A2 варианти за `reading_text` + `grammar_table`

**Публичният домейн показва само A1, но leak-ът чупи A1 визуално.** Затова това е merge-блокер, не follow-up.

Вече на master (от Нина): A2 override-ва `reading_text` + `grammar_table` без префикс — A1 вече ползва A2 варианти за тези 2 типа. B1 го прави по-зле.

`InfoHighlightOnly.tsx` / `b1-info-highlight` — дефиниран, **не е регистриран**. Безобидно.

`vocabulary-augment.d.ts` — module augmentation за `voiceGender` на vocab. Оставя се (B1 файл).

### Какво НЕ е пипал Алекс (добре)
`src/components/**`, `src/lib/**`, `src/app/**`, `src/content/shared/**`, `registry.ts`, `ui.ts`, `package.json` — чисто.

### Gating — fail-open, внимавай
`getEnabledLevelsForHost` (в `src/lib/enabledLevels.ts`) връща **всички** нива за всеки хост, който не е изрично изброен в `A1_ONLY_HOSTS`. Там има само един домейн (`bulgarian-for-refugees-unhcr.vercel.app`).

Тоест B1 е скрит **само** на този един домейн. Всеки друг адрес — preview линкове, localhost, вътрешни домейни, линкът, който Philip праща на клиента — показва B1. Ново domain по подразбиране показва всичко.

Практическо следствие: регистрираните B1 уроци са видими за клиента. Renderer leak-ът пък би счупил A1 навсякъде.

### Loader регистрация — как се контролира какво вижда клиентът
`loadLesson(id)` връща `null` ако няма loader. **Това НЕ дава 404** — `src/app/lessons/[lessonId]/page.tsx` има отделен клон, който рендерира placeholder „Съдържанието на урок N все още се подготвя." + навигацията, и връща HTTP 200. Проверено емпирично на 18.08.

Значи какво е регистрирано в `B1_LESSON_LOADERS` решава дали урокът има **съдържание**, не дали е достъпен.

- master днес: loaders само `01, 02` (counts: 01=20, 02=14)
- alex: loaders `01–10`
- `B1_NAV_ITEMS` / `B1_LESSONS_METADATA` изброяват **всичките 11** и в двата клона

Следствие: в nav и на `/level/b1` се виждат картите на всичките 11 урока, а нерегистрираните показват placeholder страница. Урок 11 няма content папка (само `.gitkeep`), затова остава на placeholder дори след регистрация.

### Активи — рискът „нещо да остане некачено“
- `.gitignore` **не** изключва `public/assets` — mp3 и картинки се комитват нормално.
- `.gitattributes`: `*.mp3` са **нарочно обикновени git blob-ове, не LFS** (иначе Vercel деплойва счупени pointer файлове). `*.wav` / `*.ogg` са LFS, но не се сервират.
- Затова **мърдж на цялото дърво не губи нищо**. Cherry-pick е този, който може да разедини content от неговите mp3-та.
- `content:lint` проверява само `imageUrl` / `flagUrl` за липсващи файлове. **НЕ проверява mp3** — TTS пътищата са по конвенция, не по поле. Липсващо аудио се лови само в браузъра (Фаза 4).
- Единственото, което не се вижда от тук: дали Алекс има непушнати неща локално. **Той трябва да потвърди** `git status` clean + всичко push-нато към `origin/alex`, преди Фаза 2.

---

## Решения (Philip, преди код)

| # | Въпрос | Препоръка |
|---|---|---|
| D1 | Клон за работа | `alex-merge` от `origin/alex`, после `merge master`. Не мърджвай в текущия стар `philip`. След успех: fast-forward `philip` = `master`, или PR `alex-merge` → `master`. |
| D2 | `generate-tts.ts` | **Master печели.** Не взимай промпта/override-ите на Алекс. |
| D3 | Renderer leak | **Първо** изолирай по ниво в `ExerciseRenderer` (Philip, 1 файл), **после** мърдж на alex. Така B1 override-ите остават B1-only и се оправя A2→A1 leak. |
| D4 | QA на 10-те урока сега? | **Не.** Мърджът е интеграция. Съдържателен QA = отделни чатове след като build е зелен. |
| D5 | Пипаме ли уроците на Алекс? | Не в този поток, освен lint-disable коментарите за L01/L02. |
| D6 | Само уроци 01–06? | **Мърджни всичко.** Cherry-pick на 01–06 е капан — виж по-долу. Регистрацията: първоначално само 01–06, **преразгледано на 18.08 → регистрирани са 01–10** (виж „D6-ревизия"). |

### D6 — защо не cherry-pick на 01–06

Хронологията го изключва. Финалната шлифовка на **урок 05 и 06 е на върха на клона** (16–18 август), **след** като 07–10 вече са влезли:

```
22.07  L05 digitalise      23.07  L07      24.07  L08      10.08  L09
12.08  L10
13.08  L05 polish  ← след L10
14.08  L06 fix
16.08  L05 host corrections
16.08  L06 green-border tables
18.08  L06 grammar reference   ← върхът на клона
```

Няма момент в историята, в който „01–06 са готови, а 07–10 ги няма“. Освен това ~20 комита пипат **същите** споделени B1 файлове (`types.ts`, `components/`, `exercise-components.ts`, `i18n/b1.ts`), редувайки уроците. Cherry-pick на подмножество прескача междинните редакции на същите файлове → конфликт на почти всеки комит, и реален риск content да се разедини от своите mp3-та.

**Вместо това:** мърджни цялото дърво — всички файлове и активи влизат, нищо не се губи.

Бележка за Фаза 3: `next build` **НЕ** прави type-check — `next.config.ts` има `typescript.ignoreBuildErrors: true` и `eslint.ignoreDuringBuilds: true`. Ако искаш типова валидация, пусни `npx tsc --noEmit` отделно.

Counts за регистрация (от alex): 01=19, 02=14, 03=18, 04=10, 05=21, 06=7, 07=24, 08=12, 09=31, 10=8.
(master има 01=20 — версията на alex печели.)

**Терминология:** B1 няма урок 00 — започва от 01. „Уроци 0–6“ = **01–06, шест урока**.

### D6-ревизия (18.08, решение на Philip)

Регистрирани са **01–10**, не само 01–06. Причини:

1. Първоначалният аргумент („клиентът да не вижда неревюирани уроци") не се държи — B1 **не** е заключен в линка, който Philip праща на клиента (виж „Gating — fail-open"). Заключен е само един домейн.
2. Нерегистриран урок и бездруго не е скрит: картата му се вижда в nav и на `/level/b1`, а страницата дава placeholder, не 404.
3. QA на 07–10 изисква уроците да могат да се отварят.

Прието с ясно съзнание, че **07–10 не са минали съдържателен QA** — това е Фаза 6, не блокер тук. Урок 11 няма content папка → остава на placeholder.

---

## Фази (1 чат = 1 фаза)

Всеки агент: прочети „Факти“ + своята фаза. Спри. Обнови „Статус“ най-горе ако пипаш файла.

### Фаза 0 — този документ  · модел: евтин / текущият
Готово в planning чата. Не повтаряй git inventory.

### Фаза 1 — renderer isolation  · модел: **Sonnet**
**Преди** git merge на alex.

Файл: само `src/components/exercises/ExerciseRenderer.tsx` (Philip домейн).

Цел: lookup по ниво, не плосък merge. Псевдо:
- вземи level от `exercise.id` (`b1-…` → b1, `a2-…` → a2, иначе a1)
- търси първо в map-а на това ниво, иначе shared switch
- A2 `reading_text` override повече не удря A1
- след като alex се мърджне, B1 unprefixed override-и удрят само B1

Не пипай A2/B1 компоненти. Не чети уроци. После `npm run build`.

### Фаза 1.5 — нулиране на визуалния ефект върху A1  · модел: **Sonnet** · ГОТОВО
Фаза 1 връща A1 към споделените компоненти и това **отнема** две подобрения,
които A1 е получавал наготово от A2 форка (виж „Защо A1 се промени" в дневника).
Клиентът иска A1 да остане непроменен, затова двете подобрения са пренесени в
самите споделени компоненти:

1. `src/components/exercises/ReadingText.tsx` — при точно 2 картинки решетката е
   `gap-2 grid-cols-2 max-w-xl md:max-w-2xl mx-auto` (центрирана), както в A2
   форка. Inline `gridTemplateColumns` от `imageColumns` вече важи само при 3+
   картинки — това е поведението, което A1 показваше досега, и нито едно A1
   упражнение не ползва `imageColumns`, така че разлика няма.
2. `src/components/exercises/GrammarTable.tsx` — приема `ttsNotes` и го ползва
   като browser-TTS fallback (`ttsNotes[idx] ?? note`). `ttsNotes` вече е в
   `shared/types.ts`, но споделеният компонент го игнорираше; A1 уроци 4 и 7 го
   подават.

A2 и B1 не са засегнати — те продължават да ползват своите форкове. A2 форковете
вече са функционално идентични със споделените, тоест стават излишни, но не се
пипат (домейн на Нина).

Промпт за нов чат:
```
Прочети само docs/HANDOFF-alex-merge.md секции „Факти“ и „Фаза 1“.
Направи renderer isolation в ExerciseRenderer.tsx по D3.
Не чети уроци. Не мърджвай клонове. После build.
```

### Фаза 2 — git merge  · модел: **Sonnet** (почти само shell)
Не започвай ако Фаза 1 не е в `master` или в базата, върху която мърджваш.

```
git fetch origin
git checkout master && git pull
git checkout -b alex-merge origin/alex
git merge master
```

Преди старта: Алекс потвърждава, че всичко му е push-нато към `origin/alex`.

Конфликти — очаквай:
1. `scripts/generate-tts.ts` → **ours/master** (цял файл от master)
2. `b1-lesson-01/exercises.ts` + `b1-lesson-02/exercises.ts` → съдържание alex + върни lint-disable коментара от master ако трябва

След резолването, по D6-ревизия — в `src/content/b1/index.ts`:
- `B1_LESSON_LOADERS`: активни `01–10`
- `B1_LESSON_EXERCISE_COUNTS`: 01=19, 02=14, 03=18, 04=10, 05=21, 06=7, 07=24, 08=12, 09=31, 10=8
- **Не** пипай `B1_NAV_ITEMS` / `B1_LESSONS_METADATA` (фиксирани от syllabus-а)
- Урок 11 няма content папка — без loader, страницата дава placeholder

Не push към `origin/alex`. Не commit-вай на `master`.

Промпт:
```
Прочети само docs/HANDOFF-alex-merge.md секции „Факти“, D1, D2, D6, „Фаза 2“.
Направи alex-merge по командите там. Резолни конфликтите по таблицата.
Регистрирай уроци 01–10 в B1_LESSON_LOADERS (виж D6-ревизия).
Не пипай renderer. Не чети уроци.
```

### Фаза 3 — lint + build  · модел: **без AI / Sonnet само при грешки**
```
npm run content:lint          # пълен run; --lesson приема само номер (07), не пълен id
npm run build
```
0 errors задължително. Warnings OK. Ако lint пищи за free-writing — сложи `// content-lint-disable no-free-writing` в началото на файла, не пренаписвай упражнения.

`npm run build` **не** прави type-check (`ignoreBuildErrors: true`). За типова валидация: `npx tsc --noEmit` — има заварени грешки в `a2/types.ts`, `b1/types.ts` и някои уроци, същият pattern на двете нива, извън обхвата на този мърдж.

Промпт:
```
Прочети само docs/HANDOFF-alex-merge.md Фаза 3.
Пусни npm run content:lint и npm run build.
Поправи само това, което чупи lint/build. Не прави съдържателен review.
```

### Фаза 4 — димен smoke  · модел: **човек + евтин Sonnet само при бъг**
Браузър на `localhost:3010` (localhost показва всички нива). **Рестартирай dev сървъра** — при смяна на branch/регистрация старият процес държи стар граф. Ако видиш `ChunkLoadError` или страница без стилове → изчисти `.next` (виж „Операционен капан" в дневника), не е свързано с мърджа.

Номерата по-долу са **както се показват на сайта**. Заглавието в кода („УПРАЖНЕНИЕ 31") не е това, което се вижда: `ExerciseRenderer` отрязва числото от заглавието и слага свой пореден брояч, който брои и НОВИ ДУМИ / ГРАМАТИКА / ДИАЛОЗИ.

- **A1 урок 6 „Моето семейство" → `37. ДОПЪЛНИТЕЛНИ УПРАЖНЕНИЯ`** (раздел „Текстове и въпроси за бежанци…") — 2 картинки, центрирани. След Фаза 1.5 трябва да изглежда **точно като преди мърджа**.
- **A1 урок 10 „На път" → `34. УПРАЖНЕНИЕ`** — същият случай, единственият друг в A1.
- **A1 урок 5 „Градът и селото"** — контрола: няма 2-картинкови текстове, трябва да е буквално непроменен.
- **1 A2 урок** (напр. 9 „Професии") — A2 не се променя от Фаза 1.
- **B1 уроци 01 и 06** + аудио: по няколко mp3 (дума, граматичен ред, диалог, параграф).
- **B1 урок 05 → `21. УПРАЖНЕНИЕ` и `23. УПРАЖНЕНИЕ`** (`table_fill`) — ползват поле `labelHeader`, което споделеният тип не декларира. Ако колоната със заглавия се вижда, B1 компонентът се ползва и Фаза 1 работи.
- **B1 урок 11** — очаквай placeholder „все още се подготвя" (няма content папка). Това е нормално, не 404.

**Критерий „A1 непроменен" — вече е буквален.** След Фаза 1.5 A1 не трябва да се променя **никъде**, включително в двете упражнения по-горе. Всяка визуална разлика в A1 = спри.

Аудиото е предварително проверено детерминистично (18.08): ~1170 mp3 в B1 01–10, нула нулево-байтови, всички истински MPEG блобове, tracked в git. Затова аудио проверката може да е лека.

### Фаза 5 — PR  · модел: евтин
```
git push origin alex-merge
gh pr create --base master --head alex-merge
```
После Philip merge през GitHub. Алекс сам си rebase-ва `alex` върху новия master. **Никога** `git push origin alex`.

Промпт:
```
Прочети docs/HANDOFF-alex-merge.md Фаза 5.
Push alex-merge и отвори PR към master. Не мърджвай. Не push-вай към alex.
```

### Фаза 5 — изпълнено (2026-08-18)

`alex-merge` push-нат, [PR #12](https://github.com/Philip-Morris-Immersica/language-app-level1/pull/12)
отворен и мърджнат в `master` с **merge commit** (`9f4e18b`), не squash — така
историята на Алекс остава в master и нейният ре-синк е fast-forward, без
конфликти. Vercel preview: success. Vercel production на master: success.
1213 файла, +17312 / −1240.

`philip` е ре-синкнат (fast-forward, вече идентичен с master).

Състояние на клоновете след мърджа — **всички са пълни ancestor-и на master**,
никъде няма уникална работа: `alex` 38 назад / 0 напред, `nina-merge` 56 / 0,
`nina` 179 / 0, `philip` 0 / 0.

### Ре-синк за Алекс (на нейната машина)

`origin/alex` е ancestor на master, затова мърджът е **fast-forward** — няма как
да гръмне конфликт. Стъпките:

```
git status                 # трябва да е clean; ако не — commit + push първо
git checkout alex
git fetch origin
git merge origin/master    # fast-forward, без конфликти
git push origin alex
```

След това: изтрий `.next` и рестартирай dev сървъра (влизат 1213 файла, старият
chunk граф остава счупен — виж „Операционен капан").

Какво ще види променено и е **нарочно**:
- `scripts/generate-tts.ts` — версията на master печели (D2). Нейните промени
  сменяха глобалния Gemini промпт и hardcode-ваха B1 override-и в скрипта. B1
  MP3-тата вече са генерирани, така че нищо не се губи. Бъдещи корекции на
  произношението минават през content полетата (`ttsPrompt`, `ttsText`,
  `ttsModel`), не през скрипта.
- `ExerciseRenderer.tsx` — custom renderer-ите вече се търсят по ниво. Нейните
  override-и на непрефиксирани shared типове (`reading_text`, `dialogues`,
  `grammar_table`, …) вече важат **само за B1**, което е желаното поведение.
  Нищо в B1 не се променя визуално.
- `b1/index.ts` — уроци 07–10 са регистрирани и достъпни (без съдържателен QA).

**НЕ** прави force-push и **НЕ** push-ва към `master` или чужд клон.

### Фаза 6 — follow-up (НЕ сега)
Отделни чатове, след като PR е в master:
- Ре-синк `philip` към новия master (виж „Клонове“)
- Съдържателен QA на уроци 01–06 — по 1 урок на чат
- Пускане на 07–10: QA на урок → uncomment 1 ред loader + count → PR
- Премести B1 TTS override-и от (отхвърления) скрипт в `ttsPrompt` полета в content — по 1 урок
- По желание: префиксирай B1 override типовете (`b1-reading-text` и т.н.) за да няма unprefixed ключове
- Регистрирай или изтрий мъртвия `InfoHighlightOnly`

---

## Какво НЕ прави никой агент в този поток
- Не чети `exercises.ts` / `content.ts` на 10-те урока
- Не генерирай TTS
- Не пипай `src/content/shared/**`, `ui.ts`, `registry.ts`
- Не push към `origin/alex` или `master`
- Не force-push
- Не „оправяй“ креативните B1 компоненти по време на мърджа — те са в неговия домейн и са безопасни **след** Фаза 1

---

## Изпълнение — работен дневник

### Фаза 1 — renderer isolation (2026-08-18, същия чат като Фаза 2/3/3.5)
Направено на `philip` branch, commit `7384589`. `CUSTOM_RENDERERS` замени с
`LEVEL_CUSTOM_RENDERERS` (map по ниво) + `levelFromExerciseId()`. Lookup вече е
`LEVEL_CUSTOM_RENDERERS[level]?.[exercise.type]` вместо плосък spread. Гейт:
`npm run build` зелен (451s, exit 0). Push-нато на `origin/philip` (не master —
philip е собствен branch, разрешено).

### Фаза 2 — merge + регистрация (2026-08-18, продължение)
```
git checkout master && git pull        # 6ef12eb
git checkout -b alex-merge origin/alex # 75e1b19
git merge master --no-edit             # ci auto-merge, БЕЗ conflict markers
```
Изненада: `scripts/generate-tts.ts` **не гръмна като конфликт** — git го
auto-merge-на (промените на master и alex паднаха в непресичащи се региони),
резултатът беше **хибрид**, не "master wins". Ръчно поправено:
`git checkout master -- scripts/generate-tts.ts` → commit `921df41`.
`b1-lesson-01/02/exercises.ts` auto-merge-наха се чисто, lint-disable
коментарът оцеля без ръчна намеса.

За да влезе Фаза 1 в сила (иначе е no-op в `alex-merge`, което идва directly
от `origin/alex` + `master`, а не от `philip`): `git merge philip --no-edit`
→ commit `6f64491`, чисто, без конфликт (alex не е пипал
`src/components/**`).

Регистрация 01–06 по D6: `src/content/b1/index.ts`, commit `c054341` —
коментирани loaders + counts за 07–10, метадата/nav непипнати.

### Фаза 3 — lint + build (същия чат)
`npm run content:lint` (пълен run, не само 01–06 — по-евтино от 6 отделни
извиквания, `--lesson b1-lesson-NN` не съвпада с текущия script синтаксис
въпреки примера по-горе — приема само `--lesson NN`, не пълния id):
**8 грешки** в `b1-lesson-03` (`b1-l03-ex-23`,
`no-free-writing` — отрицателна заповед, всеки отговор уникална глаголна
форма, същата категория като вече освободените L01/L02). Поправка: същия
`content-lint-disable no-free-writing` коментар, commit `3e47ddb`. След това:
**0 errors, 32 warnings** (всички warnings извън обхвата — a1/a2/lesson-11,
предшестващи мърджа).

`npm run build` → exit 0 (491s). **Важна бележка:** `next.config.ts` има
`typescript.ignoreBuildErrors: true` + `eslint.ignoreDuringBuilds: true` —
build-ът **не прави** type-check въпреки очакването в този документ ("Build
прави type-check и на нерегистрираните 07-10"). Пуснат отделно
`npx tsc --noEmit`: съществуващи type грешки в `b1-lesson-05`,
`b1-lesson-08`, `b1/types.ts` (unprefixed `ExerciseType` mismatch — същия
pattern като A2's `types.ts`, вече чупен на master преди мърджа) +
`GrammarTable.tsx` (`compactPronouns`). Всички са **pre-existing в
`origin/alex`**, не регресия от мърджа — извън обхвата (не пипаме B1
съдържание). Философ: build gate-ът минава по буквата на плана, но не носи
защита, която планът предполагаше — flag за follow-up (Фаза 6): или включи
type-check отделно в CI, или приеми риска.

### Фаза 3.5 — детерминистични проверки (същия чат)
1. `git diff master alex-merge -- scripts/generate-tts.ts` → празно ✓.
2. `git diff --stat master alex-merge -- . ':!src/content/b1' ':!public/assets/b1-lesson-*' ':!docs/HANDOFF-alex-merge.md'`
   → само `ExerciseRenderer.tsx` (Фаза 1) + `src/i18n/b1.ts` (alex-ов домейн,
   не leak) ✓.
3. `B1_LESSON_LOADERS` съдържа точно `01-06` активни, `07-10` коментирани ✓.
4. `git log origin/alex..alex-merge` → 32 локални комита, `alex-merge` няма
   remote branch — нищо push-нато ✓.
5. `content:lint` → 0 errors (виж Фаза 3) ✓.
6. `npm run build` → exit 0 (виж Фаза 3, с уговорката за `ignoreBuildErrors`) ✓.

**Заключение: чисто.** Единствените отклонения от плана: (а) auto-merge на
`generate-tts.ts` вместо явен conflict — поправено ръчно към D2; (б) Фаза 1
влиза в `alex-merge` през допълнителен `git merge philip`, не автоматично
през `git merge master` (защото Фаза 1 не е стигнала до `master` — е само на
`philip`); (в) build gate-ът е по-слаб от очакваното (не type-checks) —
flag-нато за Philip/Фаза 6, не блокер тук.

### Независима проверка на 3.5 + D6-ревизия (2026-08-18, Opus чат)

Шестте проверки от 3.5 бяха преиграни независимо — всички се потвърждават.
Допълнително проверено, което 3.5 не покрива: renderer diff-ът е коректен, и
предпоставката му („нивото се извежда от `exercise.id`") е валидна — нито едно
упражнение в A2/B1 няма непрефиксиран id, така че никое не пада в грешно ниво.

Открити **две грешни очаквания в самия този документ** (не в изпълнението),
и двете вече поправени по-горе:
1. „Нерегистриран урок дава 404" — не дава. Дава placeholder страница с HTTP 200.
2. „Ако A1 се е сменил визуално → спри" — беше временно обърнато за 2 упражнения,
   после върнато обратно с Фаза 1.5. Виж следващата секция.

### Защо A1 се промени, въпреки че B1 е „изолирано"

Причината не е B1. Причината е A2 — и я имаше на `master` преди този мърдж.

`ExerciseRenderer` е ползвал плосък merge на custom renderer map-овете
(`{...A2_CUSTOM_RENDERERS, ...B1_CUSTOM_RENDERERS}`), без да гледа нивото.
Нина е регистрирала своите два форка под **непрефиксирани** ключове —
`'reading_text'` и `'grammar_table'` — а точно тези типове ги ползва и A1.
Затова A1 отдавна е рендиран от A2 компонентите. Никой не го е забелязал, защото
форковете са почти идентични със споделените и разликата се вижда само при
точно 2 картинки.

Фаза 1 сложи lookup по ниво. B1 се изолира — това беше целта — но същият
механизъм изолира и A2, и A1 падна обратно на споделените компоненти. Тоест A1
не „се счупи от B1"; A1 **загуби две подобрения, които е ползвал по случайност**:

| Какво | A1 преди Фаза 1 (чрез A2 форка) | A1 след Фаза 1 (споделен) |
|---|---|---|
| 2 картинки в `reading_text` | центрирани, `max-w-2xl` | разпънати на цялата ширина |
| `ttsNotes` в `grammar_table` | чист текст за browser TTS | чете сурово `notes[]` със `**`, `→`, ⚠️ |

Фаза 1.5 пренася и двете в споделените компоненти, така че A1 остава непроменен,
а B1 и A2 продължават със своите форкове. Проверено в браузъра: контейнерът в
урок 6 и урок 10 отново е `grid mb-6 gap-2 grid-cols-2 max-w-xl md:max-w-2xl mx-auto`.

Поуката за прегледите нататък: непрефиксиран ключ в `<level>/exercise-components.ts`
е тих leak към A1. Всеки нов override трябва да е с префикс (`'b1-…'`, `'a2-…'`)
или подобрението да влиза директно в споделения компонент.

Аудио, проверено детерминистично вместо в браузъра: ~1170 mp3 в B1 01–10, нула
нулево-байтови, нула под 1KB, извадка с истински MPEG frame sync (не LFS
pointer-и), всички tracked в git. Кирилица в имената има прецедент в A1 (26
файла в урок 6, работят в продукция).

**D6-ревизия изпълнена:** `B1_LESSON_LOADERS` + counts разширени до `01–10`.
Проверено с HTTP: 07/08/09/10 връщат реално съдържание (payload 100–206KB,
десетки exercise id refs), 11 остава placeholder (28KB, 0 refs).
Гейтове след промяната: `npm run content:lint` → **0 errors, 32 warnings**
(непроменено спрямо Фаза 3), `npm run build` → **exit 0** (445s).

### Операционен капан — не пускай `npm run build` докато `next dev` работи

Случи се в този чат: `npm run build` презаписа `.next`, а работещият dev сървър
на порт 3010 остана със счупен chunk граф. Резултат в браузъра: страница без
CSS + `Runtime ChunkLoadError: Loading chunk app/login/page failed`. Не е
свързано с мърджа, съдържанието или Фаза 1 — чисто артефакт от споделената
`.next` папка.

Оправяне (проверено):
```
# спри dev процеса (Stop-Process на PID-а, който слуша 3010)
Remove-Item -Recurse -Force .next
npm run dev
```
`.next/` е в `.gitignore` и нула файлове от нея са tracked, така че изтриването
е безопасно.

За следващите фази: или спирай dev сървъра преди build, или пускай build в
отделен checkout. Ако видиш `ChunkLoadError` или страница без стилове по време
на Фаза 4 — първо изчисти `.next`, преди да заподозреш мърджа.

### Фаза 4.5 — Opus финално ревю (2026-08-18)

**Заварено състояние — единствената реална находка.** Фаза 1.5 и D6-ревизията
бяха изпълнени, но стояха **некомитнати** в работната директория: промените по
`ReadingText.tsx`, `GrammarTable.tsx`, `b1/index.ts` и дневника не бяха в нито
един комит. Ако Фаза 5 беше пусната както е, PR-ът щеше да излезе **без** Фаза
1.5 (A1 щеше да регресира визуално) и **без** регистрацията на 07–10. Комитнати
в три отделни комита: `d26a9da`, `3d4a619`, `01c3801`.

Поука за процеса: „изпълнено" и „комитнато" не са едно и също. Всяка фаза, която
пипа файлове, трябва да завършва с `git status` clean, не само със зелен гейт.

**Проверки — всички чисти:**

| # | Проверка | Резултат |
|---|---|---|
| 1 | `git diff origin/master alex-merge -- scripts/generate-tts.ts` | празно — D2 спазено |
| 2 | Diff спрямо master по `shared/`, `registry.ts`, `index.ts`, `ui.ts`, `lib/`, `app/`, `db/`, конфигурации | празно |
| 3 | Diff извън B1 домейна | само `ExerciseRenderer.tsx` (Ф1), `ReadingText.tsx` + `GrammarTable.tsx` (Ф1.5), `i18n/b1.ts` (домейн на Алекс) |
| 4 | Логиката в `ExerciseRenderer` | `LEVEL_CUSTOM_RENDERERS[level]?.[type]` — истински lookup по ниво |
| 5 | Предпоставката на Ф1 (нивото се извежда от `exercise.id`) | 474 A1 + 485 A2 + 287 B1 упражнения — **нула** с грешен префикс |
| 6 | `B1_LESSON_LOADERS` + counts | 01–10 активни, counts съвпадат с плана; 11 без loader |
| 7 | `npm run content:lint` | 0 errors, 32 warnings (всички заварени в A1: уроци 05, 06, 11) |
| 8 | `npm run build` | exit 0 (504s), 53 статични страници |

**Какво добави това ревю спрямо проверката на 3.5:** предпоставката на Фаза 1
беше проверена и върху **тестовете**, не само уроците. A2 тестовете също минават
през `ExerciseRenderer`; ако някое упражнение там имаше непрефиксиран `id`, Фаза
1 щеше тихо да го прехвърли от A2 форка към споделения компонент. Всички 485 A2
упражнения (уроци + тестове) са префиксирани — рискът не съществува.

---

## Статус
- [x] Inventory + план (2026-08-18)
- [x] Гейт: Алекс потвърждава push — **не получено директно**; `origin/alex`
      tip (`75e1b19`) съвпада с fact-finding-а от същия ден, приема се като
      достатъчно за нискорисков disposable branch. Потвърди явно преди Фаза 5.
- [x] D1–D3 + D6 потвърдени от Philip (стартиращият чат = Philip)
- [x] Фаза 1 — B1 renderer isolation
- [x] Фаза 2 — merge + регистрация (01–06, после разширено до 01–10 по D6-ревизия)
- [x] Фаза 3 — lint + build
- [x] Фаза 3.5 — детерминистични проверки (доклад по-горе, чисто)
- [x] Независима проверка на 3.5 + D6-ревизия (Opus чат, 18.08)
- [x] Фаза 4 — димен тест (Philip, браузър; целият списък мина чисто)
- [x] Фаза 4.5 — Opus финално ревю (доклад по-долу)
- [x] Фаза 5 — push + PR
