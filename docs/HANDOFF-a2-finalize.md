# HANDOFF — A2 финализиране: дневник на изпълнението

> **Този файл се ПИШЕ, не се чете за инструкции.** Тук всеки агент записва
> какво е свършил. Инструкциите, фактите и промптовете са в
> [PLAN-a2-finalize.md](PLAN-a2-finalize.md).

**Задължение на всеки агент, който пипа файлове:**
1. Отбележи чекбокса си в „Статус“.
2. Напиши в своя слот в „Дневник“: какво направи, кой комит, кой гейт мина с
   какви числа, и всяко отклонение от плана.
3. Фазата **не е готова**, докато `git status` не е чист. „Изпълнено“ ≠
   „комитнато“ — това е реалният провал от предишния handoff (виж Фаза 4.5 в
   [HANDOFF-alex-merge.md](HANDOFF-alex-merge.md)).
4. Ако откриеш, че нещо в плана е грешно — **поправи плана** и го отбележи тук.
   Предишният handoff съдържаше две грешни очаквания, които никой не поправи
   навреме.

---

## Статус

- [x] Фаза 0 — inventory + план (2026-09-03)
- [x] D1–D10 потвърдени от Philip
- [x] Фаза 1 — локален мърдж + гейтове → [PR #15](https://github.com/Philip-Morris-Immersica/language-app-level1/pull/15) отворен
- [x] Фаза 1 — Philip: локална браузър проверка + **мърдж на PR #15 (2026-09-03, комит `f31c252`)**
- [x] Ре-синк на `philip` след PR #15 — `git merge origin/master`, 68 файла, без конфликти
- [x] Фаза 2 — pretranslate (2а кръпка, 2б пускане) · Чат 1 (2026-09-03)
- [x] Фаза 3 — раздели, пилот `a2-lesson-01` · Чат 2
- [x] Фаза 3.5 — Philip гледа пилота → поиска по-малко и по-големи раздели, 6 → 4 (комит `33f8da2`)
- [x] Фаза 4 — раздели, уроци 00, 02–10 · **работата е в комит `0010c46`, чието съобщение я скрива** (виж бележката по-долу)
- [x] Фаза 5 — празненства · комит `e7f2d68`, 11 A2 id-та в `CELEBRATION_ENABLED_LESSONS`
- [x] Фаза 6 — гейтове + финален PR · 2026-09-03 → [PR #16](https://github.com/Philip-Morris-Immersica/language-app-level1/pull/16) отворен (base `master`, head `philip`)
- [x] Фаза 8 — **преместена пред мърджа** (виж дневника): 8a линт правила, 8b правила в `.cursor/rules`, 8c и 8e отпадат
- [ ] Фаза 7 — Philip тества Vercel preview и мърджва (**след** Фаза 8, същият PR #16)
- [ ] Нина се ре-синква (един път, след мърджа — няма да прави корекции междувременно)

---

## Дневник

### Фаза 0 — inventory + план · 2026-09-03, planning чат

Одит какво добави финализиращият pass на A1 и какво липсва на A2. Резултатът е
в „Факти“ в плана. Ключовите находки: pretranslate кешът има **0** A2 записа,
A2 има **0** `sectionStart` (A1 има 56 в 11 урока), нито един A2 урок не е в
`CELEBRATION_ENABLED_LESSONS`. `grammarReference` и `instructionKey` в A2 са
**готови** — A2 дори е пред A1 по `instructionKey` (75 срещу 54).

Документът първоначално беше един файл, който смесваше план и дневник (по
образеца на `HANDOFF-alex-merge.md`). Разделен на два по искане на Philip:
планът е статичен и се чете, дневникът расте и се пише. Причина: агент за Фаза 4
трябва да прочете ~80 реда, не 505.

### Фаза 1 — локален мърдж на A2 · 2026-09-03, planning чат

```
git checkout master && git pull            # e5564c8
git checkout -b a2-merge origin/nina       # ccb3173
git merge master --no-edit                 # exit 0, НУЛА конфликта
npm run content:lint                       # 0 errors, 32 warnings
npm run build                              # exit 0, 542s, 53 страници
git push origin a2-merge                   # нов клон, dc8216f
gh pr create --base master --head a2-merge # PR #15
```

Шест детерминистични проверки, всички чисти — таблицата е в „Факти → Мърджът е
чист“ в плана. `master` е **непокътнат** (`e5564c8`), PR #15 е `mergedAt: null`.

**Отклонение от очакването:** мърджът на Алекс беше труден заради
`scripts/generate-tts.ts`, който git auto-merge-на в хибрид. Тук такъв риск
нямаше — пресечното множество между файловете на Нина и файловете на master е
празно. Затова нула конфликта не е изненада, а следствие.

**Капан, който хванах:** `git checkout -b a2-merge origin/nina` слага
`origin/nina` като upstream — един разсеян `git push` без аргументи би стрелял
по чужд клон. Пренасочено веднага след push-а:
`git branch --set-upstream-to=origin/a2-merge a2-merge`. Проверено, че
`origin/nina` е непроменен (`ccb3173`).

**Какво остава на Philip преди мърджа:** стъпка 6 от
[philip-team-lead](../.cursor/rules/philip-team-lead.mdc) — локална браузър
проверка. Конкретният списък е в [CHECK-a2-merge-pr15.md](CHECK-a2-merge-pr15.md).

### Доуточнение на Фаза 1 — какво пропуснах при първия преглед

Първата ми проверка беше „всичките 68 файла са в домейна на Нина“ и тя минава.
Но домейнът не е същото като поведението — **два от файловете са компоненти, не
съдържание**, и това го изпуснах:

1. `src/content/a2/components/A2WorkbookFillBlank.tsx` — **нов** компонент,
   регистриран в `exercise-components.ts` под **непрефиксирания** shared тип
   `'workbook_fill_blank'`. В мърджа на Алекс точно този модел беше описан като
   „тих leak към A1“.
   **Проверено: не изтича.** `ExerciseRenderer.tsx:185-186` прави търсене по
   ниво (`LEVEL_CUSTOM_RENDERERS[level]?.[exercise.type]`, нивото се извежда от
   префикса на id-то). A1 упражненията имат непрефиксирани id-та → нивото е
   `a1` → няма такава карта → падат на споделения компонент. Тоест A1 е
   безопасно **по конструкция**, не по случайност.
2. `src/content/a2/components/A2ReadingText.tsx` — променено разположение при
   `imageColumns: 3` (от `grid-cols-2 md:grid-cols-3` на
   `grid-cols-3 max-w-3xl mx-auto`) и махнати `rounded-lg shadow-sm` при
   `imageEqualHeight`. Единственото A2 упражнение, което ползва тези флагове, е
   `a2-l10-tekst-elena` — значи промяната се вижда само там.

Поука: „файлът е в домейна на автора“ отговаря на въпроса *позволено ли е*, не
на *какво прави*. Компонентите и `exercise-components.ts` изискват отделен
поглед, дори когато са в чуждия домейн.

### Операционен капан, хванат в този чат

`next dev` оставя осиротяло дете, което държи порт 3010 и преживява
`Stop-Process` на родителя. Резултат: localhost сервира стара версия от **друг
клон** и изглежда счупен, а рестартът дава `EADDRINUSE`. Диагностиката е в
„Операционни капани“ в плана. Правилото е: преди да гледаш localhost, провери
`git branch --show-current`.

### Фаза 2 — предварителни преводи · 2026-09-03, Чат 1

**2а — кръпка на `scripts/pretranslate.ts` (generic `--level`):**
- Нов флаг `--level a1|a2|b1|b2`, по подразбиране `a1` (backward-compatible).
  Невалидна стойност → грешка + `process.exit(1)`.
- Импортира и `<LEVEL>_LESSONS_METADATA` + `<LEVEL>_TEST_LOADERS` за всичките
  4 нива (`@/content/a1`, `a2`, `b1`, `b2`) и ги маппва в
  `LEVEL_LESSONS_METADATA` / `LEVEL_TEST_LOADERS` — избор по `level` вместо
  твърдия внос на `A1_LESSONS_METADATA`.
- Glossary pass (`buildGlossary`) и `--all` цикъла по уроци сега четат
  `LEVEL_LESSONS_METADATA[level]` вместо `A1_LESSONS_METADATA`.
- Test id вече е `test-${level}-${n}`, а броят тестове се взима динамично от
  `Object.keys(LEVEL_TEST_LOADERS[level]).length` (не твърдо `<= 6`).
- Нормализация на lesson id: ако аргументът вече съдържа `lesson-`
  (напр. `a2-lesson-03`, или пълно A1 id `lesson-05`) — ползва се както е,
  без втори префикс. Иначе: `lesson-${padStart}` за `a1`,
  `${level}-lesson-${padStart}` за останалите.
- Нормализация на test id аналогично: `test-` префикс се разпознава общо,
  не само `test-a1-`.
- `TranslationEntry`/останалата логика (глосар, кеш, batch превод, cost
  tracking) е непроменена — само точките на избор на ниво.

**Гейт 2а (минал):**
```
npm run pretranslate -- --level a2 --all --dry-run
```
→ изведе точно **11 урока** (`a2-lesson-00`…`a2-lesson-10`) + **6 теста**
(`test-a2-1`…`test-a2-6`), с правилните brой низове/лесон.
```
npm run pretranslate -- --all --dry-run
```
(без `--level`) → изведе **A1** непроменено (`lesson-00`…, `test-a1-*`
логиката на A1 остава same, glossary „all 12 A1 lessons").

**2б — пускане:**
```
npm run pretranslate -- --level a2 --all
```
exit code 0, elapsed ~121 минути (7 250 187 ms). Модел `gpt-5.5`,
1 017 861 input + 503 720 output токена, реален разход **$20.20**.
Обхванати: Step 0 глосар (1125 уникални граматични термина за A2, 951 нови),
всичките 11 A2 урока (`a2-lesson-00`…`10`) и всичките 6 A2 теста
(`test-a2-1`…`6`).

**Резултат в `translations.json`:** baseline при `HEAD` (преди тази фаза,
включва по-ранни dry-run resave-и без съдържателна промяна) = **2649**
записа. След пускането = **5583** записа. **Нови записи: 2934.**

**Гейт от плана (минал):**
```
git diff --stat
```
→ показва **само**:
```
scripts/pretranslate.ts              | 75 +-
src/i18n/generated/translations.json | 23472 +++++++++++++++++++++++++++++++++
2 files changed, 23532 insertions(+), 15 deletions(-)
```
**Нула** промени в `src/content`, нищо в `scripts/generate-tts.ts`.

**Забележка (не отклонение от плана, но си струва да се знае):** преди тази
фаза `translations.json` вече показваше „M" в `git status` спрямо `HEAD`
заради CRLF/LF нормализация (`git diff`/`--numstat` показваха 0 реални
промени) — не съдържателна промяна, преживяло от преди тази фаза. Пускането
на скрипта презаписва файла коректно; гейтът по-горе го покрива.

**Комит:** `179fdc5` — "feat(i18n): generic --level flag for pretranslate.ts +
full A2 pre-translation pass" на branch `philip`. `git status` чист след
комита.

### Фаза 3 — раздели, пилот `a2-lesson-01` · 2026-09-03, Чат 2

Добавих `sectionStart` в `src/content/a2/lessons/a2-lesson-01/exercises.ts`
(структурата прочетена икономично през Grep за `id|type|title|order` +
няколко целеви reads около граматичните блокове, за да разбера темата им —
не целият файл наведно).

**Първи опит: 6 секции.** Първата версия имаше 6 секции (една от тях само с
**1** упражнение). Philip прегледа на localhost (Фаза 3.5) и поиска по-едро
групиране — по-малко секции, не непременно равен обем, но заглавията да пасват
на съдържанието. Преработих до **4 секции** (виж таблицата долу); премахнах
`sectionStart` от `a2-l01-ex-12` и `a2-l01-ex-19` (сливат се в съседна секция)
и обнових заглавие/подзаглавие на `a2-l01-dialozi-03` и `a2-l01-ex-17`, за да
опишат по-широкия обхват.

**Окончателният списък раздели (еталон за Фаза 4):**

| # | `title` (bg) | `theme` | Брой упр. | Обхват (order) | Първо упражнение |
|---|---|---|---|---|---|
| 1 | Телефони и телефонни разговори | `dialogue` | 7 | 1–7 | `a2-l01-ex-01` |
| 2 | Кратки винителни местоимения | `grammar` | 6 | 8–13 | `a2-l01-gramatika-01` |
| 3 | Обаждане за работа и за апартамент под наем | `dialogue` | 8 | 14–21 | `a2-l01-dialozi-03` |
| 4 | Телефонът в ежедневието | `reading` | 3 | 22, 26–27 (order 24, 26, 27) | `a2-l01-ex-17` |

**Забележка за границите:** реалният ред на рендиране е **позицията в масива**,
не полето `order` — `a2-l01-gramatika-03` има `order: 11`, но стои в масива
**след** `a2-l01-gramatika-02` (`order: 12`); секцията се държи по позиция, не
по `order`, затова секция 2 обхваща и двете като едно цяло. Не пипнато — само
информация за следващата фаза.

**Обосновка на 4-те секции (защо не 6, защо тези граници):**
- Секция 2 (граматика 8–13) е **една** непрекъсната тема — кратки винителни
  местоимения + глаголи с преки допълнения като лексика за упражнението ѝ.
  Делене на 3 части (по буквалните сигнали от плана: „ГРАМАТИКА 1 → глаголи →
  ГРАМАТИКА 2/3") щеше да разкъса една тема с генерични заглавия.
- Секция 3 (14–21) слива „ДИАЛОЗИ 3 + двете обяви" (буквално „модел от
  ДИАЛОЗИ 3" по коментар в кода) с продължението на упражненията за
  местоимения (`ex-12/13/14`) — `ex-14` **е** телефонен диалог с местоимения,
  значи темата продължава директно, не е изкуствено слепена.
- Секция 4 (22, 26–27) слива „Телефонни номера" (`ex-17`, включва и личен
  въпрос „Колко често използвате телефона") с текста за Мария — двете говорят
  за **едно и също**: ежедневна употреба на телефона. Само 3 упражнения, но
  тематично плътна, не разредена.
- Резултат: 4 секции, размери 7/6/8/3 — не еднакви, но всяка е смислена сама
  по себе си (виж изискването на Philip: не е проблем размерът да не е равен).

**Гейтове (след преработката, спрямо истинската база `d637abe` — commit-ът
преди всяка Фаза-3 промяна, за да покрие и премахнатите/променените секции):**
```
git diff --numstat d637abe -- src/content/a2/lessons/a2-lesson-01/exercises.ts
→ 84  0  src/content/a2/lessons/a2-lesson-01/exercises.ts   (изтрити = 0 ✅)

npm run content:lint -- --lesson 01
→ a2-lesson-01: 0 errors, 0 warnings (единствените 2 warnings в изхода са в
  A1 lesson-01, несвързани с тази промяна)
```

**Празненства за пилота (изнесено от Фаза 5, само за `a2-lesson-01`):** по
искане на Philip, за да може Фаза 3.5 да провери и празненствата, добавих
`'a2-lesson-01'` в `CELEBRATION_ENABLED_LESSONS` в `src/lib/celebration.ts`
(1 ред, изцяло data-driven механизъм — виж „МЕХАНИКА → 3“ в плана, нищо друго
за писане). Гейт: `git diff --numstat HEAD -- src/lib/celebration.ts` → `2 0`.
**Фаза 5 остава да добави останалите 10 A2 id-та** (`a2-lesson-00`, `02`–`10`)
след като те получат `sectionStart` в Фаза 4 — не дублирай `a2-lesson-01`.

**Commit-и:** два, само с explicit paths, branch `philip`:
1. `9599858` — първият опит (6 секции) — **историческа референция, надминат от commit 2**.
2. `179fdc5`/`0137d20` — паралелният Фаза-2 чат комитна междувременно (не мой).
3. следващият commit тук — преработката на 4 секции + `celebration.ts` +
   този запис в handoff-а. `scripts/pretranslate.ts`,
   `src/i18n/generated/translations.json`, `docs/_pretranslate-a2-run.log` не
   са пипани от мен на никакъв етап.

**За повторна проверка (Philip):** `http://localhost:3010/lessons/a2-lesson-01`
— 4 разгъваеми части (не 6), поздрав/toast след последното проверимо
упражнение във всяка част, финален модал след „Преговор". Провери `en`/`ar`
превода на новите заглавия за секция 3 и 4.

### Фаза 4 — раздели, уроци 00, 02–10 · 2026-09-03, Чат 3

Работата е свършена и е чиста, но **комитът е озаглавен погрешно**:
`0010c46 docs(a2): tweak plan wording (7/6/8/5, A1/celebration cross-ref)`
съдържа не корекция на плана, а **цялата Фаза 4** — 940 вмъкнати реда в 10
урока. Агентът е събрал в един комит и промяната по документа, и съдържателната
работа. Не пренаписвам историята, защото комитите вече са базата на следващите,
но следващият, който търси Фаза 4, няма да я намери по съобщението. Затова е
записано тук.

Гаранцията „нула изтрити реда“ — проверена по файл, минава навсякъде:

| Урок | Вмъкнати | Изтрити |
|---|---|---|
| a2-lesson-00 | 105 | **0** |
| a2-lesson-02 | 105 | **0** |
| a2-lesson-03 | 84 | **0** |
| a2-lesson-04 | 84 | **0** |
| a2-lesson-05 | 84 | **0** |
| a2-lesson-06 | 105 | **0** |
| a2-lesson-07 | 105 | **0** |
| a2-lesson-08 | 75 | **0** |
| a2-lesson-09 | 84 | **0** |
| a2-lesson-10 | 105 | **0** |

Раздели по урок след Фаза 3.5 (по-малко и по-големи, както поиска Philip):
00 → 5, 01 → 4 (пилот), 02 → 5, 03 → 4, 04 → 4, 05 → 4, 06 → 5, 07 → 5,
08 → 4, 09 → 4, 10 → 5. **Общо 49.**

Пилотният урок има два комита: `9599858` слага 6 раздела (117 вмъкнати, 0
изтрити), а `33f8da2` ги свежда до 4 по обратната връзка от Фаза 3.5 (22
вмъкнати, 55 изтрити). Изтритите 55 реда са **само** от `sectionStart` блокове
— проверено ред по ред, нито един не е от съществуващо съдържание. Това е
единственото място в цялата фаза с изтрити редове и то е оправдано.

### Фаза 5 — празненства · 2026-09-03, Чат 3

Комит `e7f2d68`, един файл — `src/lib/celebration.ts`, 11 вмъкнати реда.
`CELEBRATION_ENABLED_LESSONS` съдържа **11** A2 id-та, тоест урок 00 е включен
по D9. Понеже всичките 11 урока вече имат `sectionStart`, планът на
`buildCelebrationPlan()` няма да е празен за нито един от тях.

### Фаза 6 — гейтове + финален PR · 2026-09-03, Чат 3

**Капан, хванат преди build:** `Get-NetTCPConnection -LocalPort 3010` показа
активен `Listen` на PID `77764` — точно осиротелият `next dev` от
„Операционни капани" в плана, въпреки че `Get-Process node` не показваше нищо
и планираният dev процес отдавна е бил рестартиран/затворен в чужд чат.
`Stop-Process -Id 77764 -Force` + `Remove-Item -Recurse -Force .next` преди
`npm run build`, за да не се чупи споделената `.next` папка.

**Гейт 1 — `npm run content:lint`:**
```
Total: 0 errors, 32 warnings
```
Разбивка на 32-те (проверена **и на база `e55ea6a`, преди всяка Фаза-4
промяна** — идентични, значит Фаза 4 не е добавила нито едно ново warning):
22 в A1 (`lesson-01` ×2, `lesson-02` ×8, `lesson-03` ×2, `lesson-05` ×7,
`lesson-06` ×1, `lesson-11` ×2) + 10 в A2 (`a2-lesson-04` ×2, `a2-lesson-06` ×1,
`a2-lesson-07` ×6, `a2-lesson-10` ×1). **Отклонение от плана:** планът твърдеше
„32 заварени A1 warnings" (всичките в A1) — на практика 10 от 32-те са
пре-съществуващи A2 warnings (`true-false-mix`, `example-points-mismatch`,
`instruction-period`), несвързани със `sectionStart` (потвърдено чрез
`git stash` + повторен lint на база `e55ea6a`: същите 10 A2 warnings, същите
ID-та, преди Фаза 4 да пипне файловете). Планът е леко неточен по състав, не по
брой — коригирано тук вместо в самия план, за да не се губи историята му.

**Гейт 2 — `npm run build`** (dev сървър спрян, `.next` изчистен преди старт):
```
✓ Compiled successfully in 53s
✓ Generating static pages (53/53)
exit_code: 0, elapsed ~561s
```

**Гаранция №1** — `git diff --numstat e55ea6a -- <lesson>/exercises.ts` за
всичките 10 Фаза-4 урока: **0 изтрити** навсякъде (таблицата е в записа за
Фаза 4 по-горе, потвърдена повторно на база `e55ea6a`).

**Гаранция №2** — `git diff --stat origin/master -- . ":!src/content/a2/lessons/*/exercises.ts" ":!src/lib/celebration.ts" ":!src/i18n/generated/translations.json" ":!scripts/pretranslate.ts" ":!docs/PLAN-a2-finalize.md" ":!docs/HANDOFF-a2-finalize.md"`
→ показва **само** `docs/CHECK-a2-merge-pr15.md` (131 вмъкнати реда). Проверих
историята: `git log -- docs/CHECK-a2-merge-pr15.md` → комит `018193e`, направен
в **Фаза 0/1**, преди тази сесия, извън обхвата на изключенията в плана просто
защото файлът не съществуваше, когато е писан изключващият списък. Само
документация (browser QA checklist), нула код/съдържание — безобидно
разминаване с буквата на гейта, не съдържателен пропуск.

**PR:** [#16](https://github.com/Philip-Morris-Immersica/language-app-level1/pull/16)
— „A2 finalize: sectionStart on all 11 lessons + completion celebrations",
base `master` ← head `philip`. Описанието съдържа: списък раздели по урок,
numstat таблица за всичките 11 урока (10 от Фаза 4 + `a2-lesson-01` копирано от
Фаза 3 без прекалкулация), и гейт-таблицата по-горе. **PR-ът НЕ е мърднат** —
само отворен, съгласно твърдите ограничения.

**Push:** `git push origin philip` → `d637abe..8c4c781`. Нищо към
`origin/nina`, нищо към `master` директно.

### Фаза 8 — линт правила + правила в `.cursor/rules` · 2026-09-03, planning чат

**Защо преди мърджа, а не след него.** Philip реши да събере Фаза 8 в **същия**
PR #16: Нина няма да прави корекции веднага, значи един мърдж и един pull за нея
е по-евтино от два. Задължителното следствие, което всеки следващ агент трябва
да знае: **`head` на PR #16 е `philip`, затова всеки комит на този клон влиза в
PR-а автоматично.** Работата е направена преди клика, не след него.

**8a — две нови правила в `scripts/content-lint.ts`** (R18, R19), и двете
`severity: 'warn'`, за да не могат да блокират гейта `0 errors`:

- `section-translations` — за всеки `sectionStart`: липсващ език в `titleI18n` /
  `subtitleI18n` (шестте са `en, fr, ar, fa, uk, ru`), `bg` вкаран по погрешка в
  I18n записа, `subtitleI18n` без български `subtitle`, невалиден `theme`,
  markdown в заглавието.
- `instruction-key-exists` — всеки `instructionKey` (включително вложените, чрез
  `walkStrings`) трябва да съществува в `UI_TRANSLATIONS` и да има всичките 7
  езика. `UI_TRANSLATIONS` се зарежда веднъж в `main()` през
  `loadUiTranslations()`; ако вносът не мине, правилото се пропуска тихо вместо
  да гърми.

**Резултат — и двете чисти:**
```
npm run content:lint -- --rule section-translations     → ✓ clean
npm run content:lint -- --rule instruction-key-exists   → ✓ clean
npm run content:lint                                    → 0 errors, 32 warnings
```
32-те warnings са байт за байт същите като преди — новите правила добавят нула.

**Проверка, че правилата не са тихо мъртви** (реалният риск при „clean“ изход —
сгрешен rule id или неуспешен внос води до 0 находки и лъжливо зелено): пуснах
двете с временно добавен несъществуващ език `zz` в списъците. Резултат:
`section-translations` даде **209** находки → обходило е **105 раздела** (56 A1
+ 49 A2, 104 от тях с подзаглавие); `instruction-key-exists` даде **128**
находки → толкова употреби на `instructionKey` резолват коректно. `zz` е върнат
веднага. Тоест „clean“ означава: **всичките 105 раздела имат всичките 6 езика за
заглавие и подзаглавие, и всичките 128 `instructionKey` имат всичките 7.** Това
е автоматичният отговор на въпроса „как да проверя преводите, като ръчно е
невъзможно“.

**8c — речниковите глоси: отпада, не е работа.** Планът я водеше като липса и за
A1, и за A2. Проверих с еднократен скрипт: `content.vocabulary[].bulgarian` се
събира от `pretranslate.ts:324`, значи глосата идва от кеша по време на работа.
Покритие: **A1 806/806**, **A2 789/789**. Единственото несъответствие (`хиляда`,
`lesson-05`) е ръчен запис в `translationOverrides.ts` — скриптът коректно го
пропуска като „вече покрито“. `VocabularyItem.translation` е празно **по дизайн**
на всички нива; не се попълва ръчно. Записано в `i18n-translations.mdc` и в
`level-finalize.mdc`, за да не се преоткрива.

**8e — публичното пускане на A2: отпада, кодът вече е верен.** `A1_ONLY_HOSTS`
съдържа само `bulgarian-for-refugees-unhcr.vercel.app`, а `enabledLevels.ts` е
fail-open. Значи A2 се вижда на `language-app-level1.vercel.app` (средата, в
която клиентът тества преди одобрение) и **не** се вижда на публичния UNHCR
линк. Точно желаното поведение — нула промени.

**8b — правилата:**

| Файл | Промяна |
|---|---|
| `.cursor/rules/level-finalize.mdc` | **Нов.** Дестилира целия поток: четирите стъпки, инкременталният кеш и защо не се отлага, гаранцията „0 изтрити реда“, оперативните капани, редът спрямо автора |
| `.cursor/rules/nina-a2.mdc` | §7 раздели (нейни са, но корекция по текст не ги пипа; структурна промяна иска преглед на границите; нов урок се оставя без `sectionStart`), §8 какво става с превода при промяна на български текст, нови линт команди |
| `.cursor/rules/alex-b1.mdc` | §7 **B1 НЕ получава раздели сега** (D11) — идват с финализиращия pass, за да е консистентен тонът; §8 преводи; поправено, че `--lesson` иска номер, не пълно id |
| `.cursor/rules/philip-team-lead.mdc` | Оперативните капани (dev/build конфликт по `.next`, осиротелият порт 3010), новите линт команди в стъпка 4, секция за финализирането + състояние по нива |
| `.cursor/rules/i18n-translations.mdc` | Трети слой (pretranslate кешът) + поведението на ключа = суров български стринг + двете нови линт правила. Остарелият текст „A2 folds hand-translation into its pending feedback pass“ е заменен с реалното състояние |

**Ново решение D11** (записано и в плана): B1 получава раздели с финализиращ
pass върху цялото ниво, не докато Алекс пише уроците.

### Фаза 7 — Philip тества и мърджва

Preview линкът се сменя с всеки комит — вземай го от Vercel check-а на
[PR #16](https://github.com/Philip-Morris-Immersica/language-app-level1/pull/16),
не от записан тук URL. Проверка на състоянието:
```
gh pr view 16 --json mergeable,mergeStateStatus,statusCheckRollup
```

_(Philip попълва тук: какво провери в preview-то, има ли находки)_
